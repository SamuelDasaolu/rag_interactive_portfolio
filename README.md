## To run, pull this repo
#### Then run
```bash
pip install -r requirements.txt
streamlit run app.py
```
#### Add your GEMINI API KEY in the side bar | Preferably a paid key
#### If you want to use a different material to personalize, add your own $DOCUMENTS\ i.e\ biography $ in the side bar
